DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Resend (email verification + future notification emails) — resend.com
RESEND_API_KEY=
# "Name <address>" — the sandbox address (onboarding@resend.dev) only sends
# to your own Resend account email until you verify a custom domain.
EMAIL_FROM=LunaFlow <onboarding@resend.dev>
# Used to build the verification link sent in the email.
FRONTEND_URL=http://localhost:5173
